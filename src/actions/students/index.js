import createStudent, { CREATE_STUDENT } from './create'
import fetchStudent, {FETCHED_ONE_STUDENT} from './fetch'
import evalStudent, {EVAL_STUDENT} from './evaluate'
import updateStudent, {UPDATE_STUDENT} from './update'

export {
  createStudent,
  CREATE_STUDENT,
  fetchStudent,
  FETCHED_ONE_STUDENT,
  evalStudent,
  EVAL_STUDENT,
  updateStudent,
  UPDATE_STUDENT,
}
