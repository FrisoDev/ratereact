import createStudent, { CREATE_STUDENT } from './create'
import fetchOneStudent, {FETCHED_ONE_STUDENT} from './fetch'
import evalStudent, {EVAL_STUDENT} from './evaluate'
import updateStudent, {UPDATE_STUDENT} from './update'
import deleteStudent, {DELETE_STUDENT} from './destroy'

export {
  createStudent,
  CREATE_STUDENT,
  fetchOneStudent,
  FETCHED_ONE_STUDENT,
  evalStudent,
  EVAL_STUDENT,
  updateStudent,
  UPDATE_STUDENT,
  deleteStudent,
  DELETE_STUDENT,
}
